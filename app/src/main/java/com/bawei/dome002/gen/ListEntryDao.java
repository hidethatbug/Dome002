package com.bawei.dome002.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bawei.dome002.entry.ListEntry;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LIST_ENTRY".
*/
public class ListEntryDao extends AbstractDao<ListEntry, Void> {

    public static final String TABLENAME = "LIST_ENTRY";

    /**
     * Properties of entity ListEntry.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property CommodityId = new Property(0, int.class, "commodityId", false, "COMMODITY_ID");
        public final static Property Count = new Property(1, int.class, "count", false, "COUNT");
    }


    public ListEntryDao(DaoConfig config) {
        super(config);
    }
    
    public ListEntryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LIST_ENTRY\" (" + //
                "\"COMMODITY_ID\" INTEGER NOT NULL ," + // 0: commodityId
                "\"COUNT\" INTEGER NOT NULL );"); // 1: count
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LIST_ENTRY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ListEntry entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCommodityId());
        stmt.bindLong(2, entity.getCount());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ListEntry entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCommodityId());
        stmt.bindLong(2, entity.getCount());
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public ListEntry readEntity(Cursor cursor, int offset) {
        ListEntry entity = new ListEntry( //
            cursor.getInt(offset + 0), // commodityId
            cursor.getInt(offset + 1) // count
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ListEntry entity, int offset) {
        entity.setCommodityId(cursor.getInt(offset + 0));
        entity.setCount(cursor.getInt(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(ListEntry entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(ListEntry entity) {
        return null;
    }

    @Override
    public boolean hasKey(ListEntry entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
